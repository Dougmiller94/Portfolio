package com.qa.assessment;

public class MagicCompass {
	
	public String foundTreasure(int xP, int yP) {
		
		
		public int xT = Treasure.findTreasure();
		public int yT = Treasure.findTreasure();
		
		
		private int xP;
		public int xDifference = xP - xT;
		private int yP;
		public int yDiffernece = yP - yT;
	
		public int m = int Math.sqrt((xDifference * xDifference) + (yDiffernece * yDiffernece));
	
		if (xTDifference == 0 && yTDiffernece == 0) {
		}
		return System.out.println("secured the treasure hommie, now you can pay rent");
		}
		else {
				System.out.println("magic Compass (AKA Apple watch) is telling you to move this way" + m);
				}
		
}
}	
		
		
		



